# DEPRECATED!
# This file exists purely to support old versions of `mitoc-ansible`,
# which still references this file as part of its build process.
# poetry is the authoritative source of dependencies (see `pyproject.toml` and `poetry.lock`)
#
blinker==1.4
Click==7.1.2
Flask-MySQL==1.5.2
Flask==1.1.4
itsdangerous==1.1.0
Jinja2==2.11.3
MarkupSafe==2.0.1
mitoc-const==0.4.1
PyJWT==2.4.0
PyMySQL==1.0.2
pytz==2021.1
raven==6.10.0
Werkzeug==1.0.1
