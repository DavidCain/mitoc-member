language: python
python:
  - "3.6"
before_install: pip install poetry
install:
  - poetry install
  - poetry install --extras=code_coverage
env:
  - FLASK_APP=autoapp.py
script:
  - black --check member
  - isort -rc --check member
  - coverage run -m flask test
after_success:
  - coveralls
